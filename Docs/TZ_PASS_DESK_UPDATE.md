# ТЕХНИЧЕСКОЕ ЗАДАНИЕ (ТЗ)
## на доработку корпоративного портала управления пропусками «PassDesk»

**Дата:** 09.12.2025
**Версия:** 2.0
**Статус:** Согласование

---

## 1. ВВЕДЕНИЕ

### 1.1. Цель документа
Определить функциональные и технические требования к доработке системы PassDesk, включая оптимизацию кода, внедрение систем распознавания документов (OCR), интеграцию с государственными реестрами (МВД), системой контроля доступа (СКУД СИГУР) и разработку модуля охраны труда.

### 1.2. Объект автоматизации
Корпоративный веб-портал PassDesk.
*   **Стек технологий:** React 18, Node.js (Express), PostgreSQL (Yandex Cloud), S3 Storage.
*   **Архитектура:** Feature-Sliced Design (FSD).

---

## 2. ФУНКЦИОНАЛЬНЫЕ ТРЕБОВАНИЯ

### 2.1. Аудит, оптимизация и безопасность

#### 2.1.1. Рефакторинг и оптимизация кода
*   **Правило 600 строк:** Провести рефакторинг компонентов, превышающих 600 строк кода (в частности, `MobileEmployeeForm.jsx`), разделив их на более мелкие функциональные модули согласно методологии FSD.
*   **Очистка кода:** Удалить неиспользуемые функции, импорты и закомментированные участки кода.
*   **Performance:** Оптимизировать рендеринг React-компонентов (useMemo, useCallback) для устранения лишних перерисовок.

#### 2.1.2. Аудит безопасности
*   **Защита API:**
    *   Внедрить Rate Limiting (ограничение частоты запросов) для эндпоинтов авторизации и загрузки файлов.
    *   Проверить все API-методы на наличие проверки прав доступа (middleware `authorize`).
*   **Работа с файлами:**
    *   Настроить время жизни подписанных ссылок (Signed URLs) S3 в зависимости от типа контента (минимизировать время доступа для чувствительных данных).
    *   Валидация MIME-типов загружаемых файлов на сервере (magic numbers).

---

### 2.2. Распознавание документов (OCR)

#### 2.2.1. Интеграция
*   Использовать API сервиса **Chandra OCR**.
*   API ключи хранить в переменных окружения (`.env`) на сервере.

#### 2.2.2. Сценарий работы
1.  Пользователь загружает скан/фото документа (Паспорт РФ, Патент) в карточку сотрудника.
2.  Система автоматически отправляет файл в API Chandra.
3.  Полученные данные (JSON) сверяются с текущими полями формы.
4.  **Автозаполнение:** Если поля формы пусты, они заполняются данными из OCR.
5.  **Валидация:** Если поля уже заполнены, но отличаются от данных OCR, система подсвечивает расхождения и предлагает пользователю выбрать корректный вариант.

---

### 2.3. Проверка в реестре контролируемых лиц (РКЛ)

#### 2.3.1. Источник данных
*   Интеграция с сервисами МВД (Реестр контролируемых лиц) через API.

#### 2.3.2. Алгоритм проверки
*   **Входные данные:** ФИО, Дата рождения, Серия и номер паспорта, Гражданство.
*   **Триггеры запуска:**
    *   Автоматически при создании/редактировании сотрудника.
    *   По расписанию (cron) для всех активных сотрудников (ежесуточно).
    *   Ручной запуск по кнопке "Проверить в РКЛ".

#### 2.3.3. Результаты и статусы
*   В сущность `Employee` добавить поле `rklStatus` (Статус РКЛ).
*   **Статусы:**
    *   🟢 `CLEAN` (Не найден в реестре) — Доступ разрешен.
    *   🔴 `RESTRICTED` (Найден в реестре) — Автоматическая блокировка пропуска, уведомление администратора.
    *   🟡 `PENDING` (Проверка/Ошибка сервиса).
*   **Отображение:**
    *   Цветовой индикатор в карточке сотрудника.
    *   Отдельная колонка в общей таблице сотрудников.
    *   Сводная таблица статистики ("Найдено в РКЛ: N человек").

---

### 2.4. Раздел "Охрана Труда" (ОТ)

#### 2.4.1. Структура раздела
Создать новый раздел для пользователей-контрагентов с возможностью загрузки документов по иерархии:

1.  **Организационно-распорядительные документы:**
    *   Доверенности, Приказы, Гарантийные письма.
2.  **Документы на ИТР (Инженерно-технических работников):**
    *   Удостоверения по ОТ, Пожарной безопасности, Электробезопасности.
    *   Привязка к конкретным сотрудникам с ролью "ИТР".
3.  **Документы на рабочих:**
    *   Допуски на высоту, наряды-допуски, протоколы проверки знаний.
4.  **Специфические документы:**
    *   Зависит от типа работ (сварка, земляные работы и т.д.).

#### 2.4.2. Функционал
*   Настройка "Обязательности" документов администратором.
*   Блокировка подачи заявок на пропуска, если отсутствуют обязательные документы раздела ОТ.
*   Инструкция для контрагентов (текстовый блок/файл в интерфейсе).

---

### 2.5. Интеграция со СКУД "СИГУР"

#### 2.5.1. Архитектура
*   Разработка модуля-коннектора (Node.js Service) для общения с сервером СИГУР.
*   Использование протокола интеграции СИГУР (Integration Protocol / Styx REST API).

#### 2.5.2. Функции управления (PassDesk -> Sigur)
*   **Создание/Обновление:** При одобрении пропуска в PassDesk автоматически создавать профиль сотрудника в СИГУР.
*   **Программирование карт:** Передача номера карты доступа.
*   **Управление доступом:**
    *   Активация доступа при начале действия пропуска.
    *   Блокировка доступа при увольнении, истечении пропуска или попадании в "Черный список" (РКЛ).

#### 2.5.3. Получение данных (Sigur -> PassDesk)
*   Импорт событий проходов (Вход/Выход) в реальном времени или по расписанию.

---

### 2.6. Модуль аналитики и посещаемости

#### 2.6.1. Интерфейс (Dashboard)
Новая страница статистики с графиками и фильтрами.

#### 2.6.2. Варианты отчетов
1.  **По объектам:** График загруженности строительных площадок/офисов (человек в час/день).
2.  **По контрагентам:** Сводная таблица численности сотрудников на объектах в разрезе компаний.
3.  **Индивидуальная статистика:**
    *   Табель учета рабочего времени (Время первого входа - Время последнего выхода).
    *   Детализация проходов через конкретные точки доступа.
4.  **Фильтры:** Период (даты), Объект, Контрагент, ФИО, Должность.

---

## 3. ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ К РЕАЛИЗАЦИИ

1.  **Backend:** Node.js v18+, Express. Использование очередей (Bull/Redis) для долгих операций (OCR, запросы к МВД, Синхронизация СКУД), если это необходимо для отзывчивости интерфейса.
2.  **Database:** Миграции через Sequelize. Новые таблицы: `safety_documents`, `rkl_checks`, `access_logs`.
3.  **Frontend:** Использование готовых компонентов Ant Design. Графики реализовать через `@ant-design/charts` или `recharts`.
4.  **Логирование:** Расширенное логирование действий интеграции (запросы к API Chandra, МВД, СИГУР) в отдельные лог-файлы или таблицу БД `system_logs`.

---

## 4. ЭТАПЫ РАБОТ

1.  **Подготовительный:** Аудит кода, рефакторинг безопасности.
2.  **Базовый функционал:** Реализация раздела "Охрана труда".
3.  **Интеллектуальный анализ:** Внедрение OCR Chandra.
4.  **Интеграции:** Подключение API МВД и СКУД СИГУР.
5.  **Аналитика:** Разработка дашбордов и отчетов.


